{
  "name": "design-mochi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev:client": "pnpm --filter @app/client run dev:client",
    "build:client": "pnpm --filter @app/client run build",
    "build-only:client": "pnpm --filter @app/client run build-only",
    "dev:admin": "pnpm --filter @app/admin run dev:admin",
    "build:admin": "pnpm --filter @app/admin run build",
    "build-only:admin": "pnpm --filter @app/admin run build-only",
    "deploy:functions": "pnpm --filter packages/functions run build && firebase deploy --only functions"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^20.17.32",
    "@vitejs/plugin-vue": "^5.1.3",
    "@vitejs/plugin-vue-jsx": "^4.0.1",
    "vite": "^6.3.5",
    "vite-plugin-inspect": "^11.0.1"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "firebase": "^10.13.1"
  }
}
